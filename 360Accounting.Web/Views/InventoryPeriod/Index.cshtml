@Html.DevExpress().GetStyleSheets(new StyleSheet { ExtensionSuite = ExtensionSuite.GridView })
@Html.DevExpress().GetScripts(new Script { ExtensionSuite = ExtensionSuite.GridView })

@model _360Accounting.Web.Models.InventoryPeriodListModel

@{
    ViewBag.Title = "Payable Periods";
}

<div class="container">
    <div class="clearfix">
        <div class="col-lg-12">
            <h3 class="page-header">
                @ViewBag.Title
            </h3>
        </div>
    </div>

    <div class="col-lg-12">
        <div class="form-group">
            @Html.LabelFor(m => m.SetOfBooks)
            @Html.DropDownListFor(m => m.SOBId, Model.SetOfBooks, new { @class = "form-control" })
        </div>
    </div>

    <div class="clearfix">
        <div id="dvList" class="col-lg-12 cusGridWrapper">
            @Html.Action("CreatePartial")
        </div>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#SOBId').change(function () {
            $('#dvList').html("Please wait..");
            $.ajax({
                url: '@Url.Content("~/InventoryPeriod/GetInventoryPeriods")',
                data: {
                    sobId: $('#SOBId').val()
                },
                datatype: 'html',
                type: 'post',
                error: function (data) { alert(data); },
                success: function (data) {
                    $('#dvList').empty();
                    $('#dvList').html(data);
                }
            });
        });
    });
</script>